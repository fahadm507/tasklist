
<h1>All Users</h1>
<table>
  <% @users.each do |user| %>
    <tr>
      <td><%= link_to "#{user.first_name}" "#{user.last_name}", user_path(user) %></td>
      <td><%= user.email %></td>
    </tr>
  <% end%>
</table>
