<h1>List of all tasks</h1>
<table>
  <% @tasks.each do |task| %>
    <tr>
      <td><%= link_to "#{task.name}" "#{task.description} - #{task.due_date}",
        user_path(task) %></td>
      <td><%= task.created_at %></td>
    </tr>
  <% end%>
</table>
